# compiled binaries (src/)
server
nonblocking_server
kevent_server
blocking_server
resp_server
resp_server_naive
test_client
dict
!dict.cpp

# compiled binaries (benchmark/)
bench_client
bench_concurrent
bench_pipeline
test_large_reply

# object files / build output
*.o
*.out
*.dSYM/

# build directories
build/
cmake-build-*/

# benchmark output (regenerate via benchmark/run_benchmarks.sh)
benchmark/*.csv
benchmark/*.png
benchmark/results/

# macOS
.DS_Store