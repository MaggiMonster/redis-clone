# compiled binaries (src/)
server
nonblocking_server
kevent_server
blocking_server
resp_server
resp_server_naive
test_client
dict
!dict.cpp

# compiled binaries (benchmark/)
bench_client

# object files / build output
*.o
*.out
*.dSYM/

# build directories
build/
cmake-build-*/

# benchmark output (regenerate via benchmark/bench_client + benchmark/latency_percentiles.py)
benchmark/*.csv
benchmark/*.png

# macOS
.DS_Store